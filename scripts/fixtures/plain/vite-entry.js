// An app entry that imports the precompiled stylesheet, as documented. scripts/pack-smoke.mjs
// builds it with Vite's defaults and checks the CSS the build emits.
import '@mortenbrudvik/waveui/styles';
